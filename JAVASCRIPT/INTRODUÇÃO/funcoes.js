function soma(a,b) { //nos parenteses são declarados os parâmetros da função
    return a + b; //se eu não for utilizar este valor da soma, apenas quero ver o resultado, posso utilizar console.log
}

soma (3,5);